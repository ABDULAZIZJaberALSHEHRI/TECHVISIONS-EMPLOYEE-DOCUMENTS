# =============================================================================
# DRMS - OCI Production Environment Variables
# Copy to .env and fill in all values before deployment
# cp .env.production.example .env
# =============================================================================

# ---------------------------------------------------------------------------
# Database (OCI MySQL - self-hosted on VM)
# ---------------------------------------------------------------------------
# If using self-hosted MySQL on the same VM:
DATABASE_URL="mysql://drms_user:YOUR_DB_PASSWORD@localhost:3306/drms"

# If using OCI MySQL Database Service (managed):
# DATABASE_URL="mysql://drms_user:YOUR_DB_PASSWORD@10.0.1.xxx:3306/drms"

# ---------------------------------------------------------------------------
# NextAuth.js Authentication
# ---------------------------------------------------------------------------
NODE_ENV=production
NEXTAUTH_URL="http://localhost:3001"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
# Generate with: openssl rand -base64 32

# ---------------------------------------------------------------------------
# Azure AD SSO Configuration
# Register app at: https://portal.azure.com > Azure Active Directory > App registrations
# Redirect URI: https://your-domain.com/api/auth/callback/azure-ad
# ---------------------------------------------------------------------------
AZURE_AD_CLIENT_ID="your-azure-ad-client-id"
AZURE_AD_CLIENT_SECRET="your-azure-ad-client-secret"
AZURE_AD_TENANT_ID="your-azure-ad-tenant-id"

# ---------------------------------------------------------------------------
# SMTP Email Configuration
# For Office 365:
# ---------------------------------------------------------------------------
SMTP_HOST="smtp.office365.com"
SMTP_PORT="587"
SMTP_USER="noreply@yourcompany.com"
SMTP_PASS=""
SMTP_FROM="DRMS <noreply@yourcompany.com>"

# For Gmail (alternative):
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM="DRMS <your-email@gmail.com>"

# ---------------------------------------------------------------------------
# File Storage (OCI Block Volume)
# ---------------------------------------------------------------------------
UPLOAD_DIR="/mnt/drms-uploads"
MAX_FILE_SIZE_MB="25"

# ---------------------------------------------------------------------------
# Application Settings
# ---------------------------------------------------------------------------
APP_NAME="TechVisions DRMS"
APP_URL="http://localhost:3001"
