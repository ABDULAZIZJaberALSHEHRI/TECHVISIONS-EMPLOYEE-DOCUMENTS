# Database
#DATABASE_URL="mysql://drms_user:your_password@localhost:3306/drms"
# Database (will be auto-created by Docker)
DATABASE_URL="mysql://drms_user:drms_local@localhost:3306/drms"
# Next AuthNEXTAUTH_URL="http://localhost:3001"NEXTAUTH_SECRET="<same-secret-you-have>" 
# Azure AD (SAME as yours - copy from your .env)AZURE_AD_CLIENT_ID="<copy-from-your-env>"
# AZURE_AD_CLIENT_SECRET="<copy-from-your-env>"
# AZURE_AD_TENANT_ID="<copy-from-your-env>"
# SMTP (optional for local testing)SMTP_HOST="smtp.gmail.com"SMTP_PORT="587"SMTP_USER="<optional>"
# SMTP_PASS="<optional>"SMTP_FROM="<optional>" 
# File UploadUPLOAD_DIR="./uploads" 
# App InfoAPP_NAME="TechVisions DRMS"APP_URL="http://localhost:3001"``` 
#**Important:** They need the **EXACT SAME** Azure AD credentials as you! 
### 5. Update Azure AD Redirect URIGo to **Azure Portal** →
#**App registrations** → Your DRMS app → **Authentication** Add this redirect URI (if not already there):```http://localhost:3001/api/auth/callback/azure-ad
# NextAuth
NEXTAUTH_URL="http://localhost:3001"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Azure AD
AZURE_AD_CLIENT_ID="your-azure-ad-client-id"
AZURE_AD_CLIENT_SECRET="your-azure-ad-client-secret"
AZURE_AD_TENANT_ID="your-azure-ad-tenant-id"

# SMTP (for email notifications)
SMTP_HOST="smtp.office365.com"
SMTP_PORT="587"
SMTP_USER="noreply@yourcompany.com"
SMTP_PASS="your-smtp-password"
SMTP_FROM="DRMS <noreply@yourcompany.com>"

# File Upload
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE_MB="25"

# App
APP_NAME="DRMS"
APP_URL="http://localhost:3001"
